---
type: daily-note
status: in-progress
created: 2026-06-01
updated: 2026-06-01
tags: [daily]
priority: high
---

# Monday, June 1st 2026

> Edited by hand or by `/plan-today`. Claude won't overwrite Notes or End-of-day sections.

> ⚠️ **Same three items as Friday — nothing moved over the weekend.** All three are byte-for-byte unchanged: onboarding still reads `build_stage: build` / `updated: 2026-05-21`, `01-select-safety/SOPs/` still doesn't exist, and the inbox note is still the only file in `06-inbox/raw-notes/`. New week, identical board. Don't add anything today — close one of these first. Start with the cheapest (Top 3 #1, ~10 min).

## Top 3
1. 🚨 **STALE — day 12 on plan, decision deadline lapsed 5 days ago** — [[driver-onboarding-workflow]]: resolve the contradiction. Default action (~10 min): open the file, flip `build_stage: build` → `requirements`, set `next_action` to `Select MVR vendor (Samba / DriverFacts / SambaSafety — pick one)`, bump `updated: 2026-06-01`. The alternative is to commit to an MVR vendor *right now* and keep `build`. Do exactly one of the two before touching anything else today.
   - *Why:* The frontmatter still lies — `dependencies` lists `MVR vendor` as TBD while `build_stage` claims `build`, and there's no DQ-file SOP for `build` to stand on. Twelve straight carries prove the bottleneck is one unmade vendor decision, not engineering work. The downgrade costs nothing, is reversible the moment you pick a vendor, and unfreezes the whole programs side.
2. 🚨 **STALE — day 8 on plan, zero movement** — Draft the DQ-file SOP via `/sop`. Create the missing `01-select-safety/SOPs/` folder, file the SOP there, backlink from [[driver-qualification-file]], and resolve its two open questions (digital-vs-paper format; retention at 3-yr minimum vs. matching insurance). ~45 min.
   - *Why:* The only item with no external blocker — doable today whichever path #1 takes, because the SOP is a named dependency in both the `build` and `requirements` versions of the onboarding program. Creating the absent folder is half the friction. Highest-leverage unblocked work on the board.
3. 🚨 **STALE — day 8 on plan, 11 days old in inbox** — Process [[2026-05-21-fleet-mgr-call-eld-gaps]] via `/process-inbox`. Split it into (a) a short-haul-exemption audit note under `01-select-safety/DOT/` covering the 49 CFR 395.1(e) check + personal-conveyance misuse, and (b) a program-idea note for the weekly HOS-violation digest under `02-programs-systems/ideas/`. ~20 min.
   - *Why:* Still the lone file in `06-inbox/raw-notes/`, now 11 days stale. Once #1 downgrades, the HOS-digest idea is the most credible candidate for the *next* active program — but only if it exists as a structured note instead of a voice-memo transcript.

## Select Safety
- [ ] Create `01-select-safety/SOPs/` and draft DQ-file SOP via `/sop` (see Top 3 #2)
  - *Why:* Folder still missing; gating dependency for the only active program regardless of its stage.
- [ ] Short-haul exemption audit against 49 CFR 395.1(e) for the two paper-log drivers from the ELD-gaps note
  - *Why:* Two drivers on paper logs is a live compliance exposure if the 150-air-mile radius / 14-hr duty math fails. Becomes a real task once Top 3 #3 splits the inbox note into an audit note.

## Programs / builds
- [ ] [[driver-onboarding-workflow]] — downgrade to `requirements` (default) **or** commit to an MVR vendor today (see Top 3 #1)
  - *Why:* Only active program; day 12 stale; the "decide or downgrade" deadline lapsed 5 days ago. Choosing neither again is exactly what keeps the programs side frozen.

## Other
- [ ] 

## Notes


## End-of-day
- What moved forward?
- What stalled?
- What goes to inbox?
